# Upstash Redis Local — environment profiles
# Copy to .env and adjust, or use: upstash-local use dev

# ── Local development (unlimited, no rate limits) ──
UPSTASH_REDIS_REST_URL=http://localhost:8000
UPSTASH_REDIS_REST_TOKEN=local-dev-token
UPSTASH_REDIS_REST_READONLY_TOKEN=local-readonly-token

# ── Cloud Upstash (production — DO NOT use in local dev) ──
# UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-cloud-token

# ── Docker Compose overrides ──
UPSTASH_TOKEN=local-dev-token
UPSTASH_READONLY_TOKEN=local-readonly-token
REDIS_PORT=6379
UPSTASH_PORT=8000

# ── Rate limit simulation (optional, for testing fallback logic) ──
# SIMULATE_QUOTA=10000
# SIMULATE_RPS=100
